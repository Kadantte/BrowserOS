{
  // Two-task subset for quick end-to-end Phoenix smoke tests.
  "name": "agisdk-mini",

  // Path to the JSONL dataset, relative to this config file.
  "dataset": "../datasets/agisdk-real-mini.jsonl",

  // eval2 supports OpenAI only in v1.
  "provider": "openai",

  // OpenAI model id. Examples: gpt-4.1, gpt-4.1-mini.
  "model": "gpt-4.1",

  // Name of the env var that holds the OpenAI API key.
  "apiKeyEnv": "OPENAI_API_KEY",

  // Path to the BrowserOS binary that spawns Chrome with CDP enabled.
  "browserosBinary": "/Applications/BrowserOS.app/Contents/MacOS/BrowserOS",

  // Port allocation for the single worker. Fixed in v1.
  "ports": {
    "cdp": 9010,
    "server": 9110,
    "extension": 9310
  },

  // Maximum wall-clock time per task, in milliseconds.
  "timeoutMs": 600000,

  // Phoenix tracing settings. enabled=false skips tracing entirely (eval still runs).
  // Cloud endpoint shown; local Phoenix server is "http://localhost:6006".
  // The auth bearer token comes from process.env[apiKeyEnv].
  "phoenix": {
    "enabled": true,
    "endpoint": "https://app.phoenix.arize.com/s/niffler92",
    "apiKeyEnv": "PHOENIX_API_KEY",
    "projectName": "browseros-eval2",
    "sessionPrefix": "agisdk"
  }
}
